# Moltbot 本地化部署文档

## 文档信息

- **项目名称**：Moltbot
- **版本**：2026.1.27-beta.1
- **文档生成日期**：2026-01-31
- **联系方式**：purpose168@outlook.com

---

## 目录

1. [项目概述](#项目概述)
2. [系统要求](#系统要求)
3. [环境准备](#环境准备)
4. [安装部署](#安装部署)
5. [配置说明](#配置说明)
6. [启动运行](#启动运行)
7. [核心功能模块](#核心功能模块)
8. [频道配置](#频道配置)
9. [守护进程配置](#守护进程配置)
10. [故障排除](#故障排除)
11. [安全建议](#安全建议)
12. [更新维护](#更新维护)

---

## 项目概述

### 项目简介

Moltbot 是一个运行在自有设备上的**个人 AI 助手**，它可以在您已经使用的渠道上与您对话，包括 WhatsApp、Telegram、Slack、Discord、Google Chat、Signal、iMessage、Microsoft Teams、WebChat，以及扩展渠道如 BlueBubbles、Matrix、Zalo 和 Zalo Personal。

### 核心特性

- **本地优先网关**：用于会话、频道、工具和事件的单一控制平面
- **多频道收件箱**：支持多种即时通讯平台
- **多智能体路由**：将入站频道/账户/对等方路由到隔离的智能体
- **语音唤醒 + 对话模式**：macOS/iOS/Android 上始终在线的语音
- **实时画布**：智能体驱动的可视化工作区
- **一流工具**：浏览器、画布、节点、定时任务、会话和 Discord/Slack 操作
- **配套应用**：macOS 菜单栏应用 + iOS/Android 节点

### 技术架构

```
WhatsApp / Telegram / Slack / Discord / Google Chat / Signal / iMessage / BlueBubbles / Microsoft Teams / Matrix / Zalo / Zalo Personal / WebChat
               │
               ▼
┌───────────────────────────────┐
│            Gateway            │
│       (control plane)         │
│     ws://127.0.0.1:18789      │
└──────────────┬────────────────┘
               │
               ├─ Pi agent (RPC)
               ├─ CLI (moltbot …)
               ├─ WebChat UI
               ├─ macOS app
               └─ iOS / Android nodes
```

---

## 系统要求

### 硬件要求

| 组件 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 2 核心 | 4 核心或更多 |
| 内存 | 4 GB | 8 GB 或更多 |
| 存储 | 10 GB 可用空间 | 20 GB 或更多可用空间 |
| 网络 | 稳定的互联网连接 | 高速互联网连接 |

### 软件要求

| 软件 | 最低版本 | 推荐版本 |
|------|---------|---------|
| Node.js | 22.12.0 | 最新 LTS 版本 |
| pnpm | 10.23.0 | 最新版本 |
| Git | 2.0+ | 最新版本 |
| 操作系统 | Linux/macOS/Windows (WSL2) | Linux/macOS |

### 操作系统支持

- **Linux**：Ubuntu 20.04+, Debian 11+, CentOS 8+, Arch Linux
- **macOS**：macOS 12.0+ (Monterey)
- **Windows**：Windows 10/11 with WSL2（强烈推荐）

---

## 环境准备

### 1. 安装 Node.js

#### Linux (使用 nvm)

```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 重新加载 shell 配置
source ~/.bashrc  # 或 source ~/.zshrc

# 安装 Node.js 22.x
nvm install 22
nvm use 22
nvm alias default 22

# 验证安装
node --version
npm --version
```

#### Linux (使用包管理器)

**Ubuntu/Debian:**
```bash
# 使用 NodeSource 仓库
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version
```

**CentOS/RHEL:**
```bash
# 使用 NodeSource 仓库
curl -fsSL https://rpm.nodesource.com/setup_22.x | sudo bash -
sudo yum install -y nodejs

# 验证安装
node --version
```

#### macOS (使用 Homebrew)

```bash
# 安装 Homebrew（如果尚未安装）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装 Node.js
brew install node

# 验证安装
node --version
```

### 2. 安装 pnpm

```bash
# 使用 npm 安装 pnpm
npm install -g pnpm

# 验证安装
pnpm --version
```

或者使用独立安装脚本：

```bash
curl -fsSL https://get.pnpm.io/install.sh | sh -
```

### 3. 安装 Git

#### Linux

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install -y git
```

**CentOS/RHEL:**
```bash
sudo yum install -y git
```

#### macOS

```bash
brew install git
```

### 4. 配置 Git（可选）

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### 5. 验证环境

```bash
# 检查 Node.js 版本
node --version  # 应该 >= 22.12.0

# 检查 pnpm 版本
pnpm --version  # 应该 >= 10.23.0

# 检查 Git 版本
git --version
```

---

## 安装部署

### 方式一：全局安装（推荐）

#### 1. 使用 npm 全局安装

```bash
npm install -g moltbot@latest
```

#### 2. 使用 pnpm 全局安装

```bash
pnpm add -g moltbot@latest
```

#### 3. 验证安装

```bash
moltbot --version
moltbot --help
```

### 方式二：从源码构建（开发模式）

#### 1. 克隆仓库

```bash
git clone https://github.com/moltbot/moltbot.git
cd moltbot
```

#### 2. 安装依赖

```bash
pnpm install
```

#### 3. 构建 UI

```bash
pnpm ui:build
```

#### 4. 构建项目

```bash
pnpm build
```

#### 5. 运行向导

```bash
pnpm moltbot onboard --install-daemon
```

#### 6. 开发模式运行

```bash
# 网关开发模式（自动重新加载）
pnpm gateway:watch

# 或直接运行
pnpm moltbot gateway --port 18789 --verbose
```

### 方式三：Docker 部署

#### 1. 创建 Dockerfile

```dockerfile
FROM node:22-alpine

# 安装 pnpm
RUN npm install -g pnpm

# 设置工作目录
WORKDIR /app

# 复制 package 文件
COPY package.json pnpm-lock.yaml ./

# 安装依赖
RUN pnpm install --frozen-lockfile

# 复制源代码
COPY . .

# 构建项目
RUN pnpm build

# 创建数据目录
RUN mkdir -p /data/.clawdbot /data/workspace

# 设置环境变量
ENV CLAWDBOT_STATE_DIR=/data/.clawdbot
ENV CLAWDBOT_WORKSPACE_DIR=/data/workspace

# 暴露端口
EXPOSE 18789

# 启动命令
CMD ["node", "dist/gateway/server.js"]
```

#### 2. 构建 Docker 镜像

```bash
docker build -t moltbot:latest .
```

#### 3. 运行 Docker 容器

```bash
docker run -d \
  --name moltbot \
  -p 18789:18789 \
  -v moltbot-data:/data \
  -e CLAWDBOT_GATEWAY_TOKEN=your-secret-token \
  moltbot:latest
```

#### 4. 使用 Docker Compose

创建 `docker-compose.yml`：

```yaml
version: '3.8'

services:
  moltbot:
    image: moltbot:latest
    container_name: moltbot
    ports:
      - "18789:18789"
    volumes:
      - moltbot-data:/data
    environment:
      - CLAWDBOT_STATE_DIR=/data/.clawdbot
      - CLAWDBOT_WORKSPACE_DIR=/data/workspace
      - CLAWDBOT_GATEWAY_TOKEN=${GATEWAY_TOKEN}
    restart: unless-stopped

volumes:
  moltbot-data:
```

启动服务：

```bash
docker-compose up -d
```

### 方式四：Render 云平台部署

项目已提供 `render.yaml` 配置文件，可以直接部署到 Render 平台。

#### 1. 准备 Render 账户

访问 [render.com](https://render.com) 并注册账户。

#### 2. 连接 GitHub 仓库

将 moltbot 仓库连接到 Render。

#### 3. 配置环境变量

在 Render 控制台中配置以下环境变量：

- `SETUP_PASSWORD`：设置密码
- `CLAWDBOT_GATEWAY_TOKEN`：网关令牌（自动生成）

#### 4. 部署

Render 会自动检测 `render.yaml` 并开始部署。

---

## 配置说明

### 配置文件位置

Moltbot 的配置文件位于 `~/.clawdbot/moltbot.json`。

### 最小配置示例

```json5
{
  agent: {
    model: "anthropic/claude-opus-4-5"
  }
}
```

### 完整配置示例

```json5
{
  // 网关配置
  gateway: {
    port: 18789,
    bind: "loopback",
    auth: {
      mode: "token",
      token: "your-secret-token"
    },
    tailscale: {
      mode: "off"  // off | serve | funnel
    }
  },

  // 智能体配置
  agent: {
    model: "anthropic/claude-opus-4-5",
    workspace: "~/clawd",
    defaults: {
      thinkingLevel: "medium",
      verboseLevel: "off",
      sendPolicy: "auto",
      groupActivation: "mention",
      sandbox: {
        mode: "non-main"
      }
    }
  },

  // WhatsApp 频道配置
  channels: {
    whatsapp: {
      enabled: true,
      allowFrom: ["+1234567890"],  // 允许的电话号码
      dmPolicy: "pairing",  // pairing | open
      groups: {
        "*": {
          requireMention: true
        }
      }
    },

    // Telegram 频道配置
    telegram: {
      enabled: true,
      botToken: "123456:ABCDEF",
      allowFrom: [],
      groups: {
        "*": {
          requireMention: true
        }
      }
    },

    // Slack 频道配置
    slack: {
      enabled: true,
      botToken: "xoxb-your-bot-token",
      appToken: "xapp-your-app-token",
      dmPolicy: "pairing"
    },

    // Discord 频道配置
    discord: {
      enabled: true,
      token: "your-discord-bot-token",
      dmPolicy: "pairing",
      allowFrom: []
    }
  },

  // 浏览器配置
  browser: {
    enabled: true,
    color: "#FF4500"
  },

  // 日志配置
  logging: {
    level: "info"
  }
}
```

### 环境变量配置

| 环境变量 | 说明 | 默认值 |
|---------|------|--------|
| `CLAWDBOT_STATE_DIR` | 状态目录 | `~/.clawdbot` |
| `CLAWDBOT_WORKSPACE_DIR` | 工作区目录 | `~/clawd` |
| `CLAWDBOT_GATEWAY_TOKEN` | 网关认证令牌 | - |
| `CLAWDBOT_SKIP_CHANNELS` | 跳过频道加载 | - |
| `CLAWDBOT_PROFILE` | CLI 配置文件 | - |
| `CLAWDBOT_NO_RESPAWN` | 禁用进程重生 | - |
| `CLAWDBOT_LIVE_TEST` | 启用实时测试 | - |

### 模型配置

#### Anthropic Claude

```json5
{
  agent: {
    model: "anthropic/claude-opus-4-5",
    anthropic: {
      apiKey: "your-anthropic-api-key"
    }
  }
}
```

#### OpenAI

```json5
{
  agent: {
    model: "openai/gpt-4",
    openai: {
      apiKey: "your-openai-api-key"
    }
  }
}
```

#### AWS Bedrock

```json5
{
  agent: {
    model: "bedrock/anthropic.claude-3-opus-20240229-v1:0",
    bedrock: {
      region: "us-east-1",
      accessKeyId: "your-access-key-id",
      secretAccessKey: "your-secret-access-key"
    }
  }
}
```

---

## 启动运行

### 1. 运行引导向导（首次安装）

```bash
moltbot onboard --install-daemon
```

引导向导会帮助您：
- 配置网关
- 设置工作区
- 配置频道
- 安装技能
- 安装守护进程（systemd/launchd）

### 2. 启动网关

```bash
# 基本启动
moltbot gateway

# 指定端口
moltbot gateway --port 18789

# 详细日志
moltbot gateway --verbose

# 开发模式
moltbot gateway --dev
```

### 3. 发送测试消息

```bash
# 发送消息到指定号码
moltbot message send --to +1234567890 --message "Hello from Moltbot"

# 与助手对话
moltbot agent --message "Ship checklist" --thinking high
```

### 4. 使用 TUI 界面

```bash
# 启动终端用户界面
moltbot tui

# 开发模式
moltbot tui --dev
```

### 5. 检查网关状态

```bash
# 运行诊断
moltbot doctor

# 检查服务状态
moltbot status
```

---

## 核心功能模块

### 1. 网关（Gateway）

网关是 Moltbot 的核心控制平面，提供 WebSocket 接口用于客户端、工具和事件通信。

**主要功能：**
- 会话管理
- 在线状态
- 配置管理
- 定时任务
- Webhook
- 控制界面
- 画布主机

**WebSocket 端点：**
```
ws://127.0.0.1:18789
```

### 2. 频道（Channels）

Moltbot 支持多种即时通讯平台，每个频道都有独立的配置和消息处理逻辑。

#### WhatsApp

- **协议**：Baileys Web
- **配置文件**：`~/.clawdbot/credentials/whatsapp.json`
- **登录**：`moltbot channels login whatsapp`

#### Telegram

- **协议**：grammY
- **配置**：需要 Bot Token
- **登录**：设置环境变量 `TELEGRAM_BOT_TOKEN`

#### Slack

- **协议**：Bolt
- **配置**：需要 Bot Token 和 App Token
- **登录**：设置环境变量 `SLACK_BOT_TOKEN` 和 `SLACK_APP_TOKEN`

#### Discord

- **协议**：discord.js
- **配置**：需要 Bot Token
- **登录**：设置环境变量 `DISCORD_BOT_TOKEN`

### 3. 智能体（Agent）

智能体是 Moltbot 的 AI 核心，使用 Pi 智能体运行时（RPC 模式）。

**主要特性：**
- 工具流支持
- 块流支持
- 会话隔离
- 多智能体路由

**工作区结构：**
```
~/clawd/
├── AGENTS.md          # 智能体配置
├── SOUL.md            # 智能体灵魂
├── TOOLS.md           # 工具配置
├── skills/            # 技能目录
│   └── <skill>/
│       └── SKILL.md
└── sessions/          # 会话数据
```

### 4. 工具（Tools）

Moltbot 提供丰富的工具集，支持各种自动化任务。

#### 浏览器控制

- 专用的 Chrome/Chromium 浏览器
- 快照、操作、上传
- 配置文件管理

#### 画布

- A2UI 推送/重置
- 评估、快照
- 智能体驱动的可视化

#### 节点

- 摄像头快照/剪辑
- 屏幕录制
- 位置获取
- 通知发送

#### 定时任务

- Cron 表达式支持
- 定时执行任务
- 唤醒功能

### 5. 守护进程（Daemon）

Moltbot 支持在后台运行，并可以随系统自动启动。

#### systemd（Linux）

```bash
# 安装守护进程
moltbot daemon install

# 启动服务
systemctl --user start moltbot

# 停止服务
systemctl --user stop moltbot

# 查看状态
systemctl --user status moltbot

# 查看日志
journalctl --user -u moltbot -f
```

#### launchd（macOS）

```bash
# 安装守护进程
moltbot daemon install

# 启动服务
launchctl start com.moltbot.daemon

# 停止服务
launchctl stop com.moltbot.daemon

# 查看状态
launchctl list | grep moltbot
```

### 6. TUI（终端用户界面）

TUI 提供了一个功能丰富的终端界面，用于与 Moltbot 交互。

**主要功能：**
- 实时聊天界面
- 会话管理
- 工具执行监控
- 日志查看
- 配置编辑

### 7. Canvas Host

Canvas Host 托管实时画布，支持智能体驱动的可视化工作区。

**主要功能：**
- A2UI 捆绑
- 画布渲染
- 交互式界面

### 8. Hooks

Hooks 系统允许在特定事件触发时执行自定义脚本。

**内置 Hooks：**
- `soul-evil`：智能体灵魂钩子
- `session-memory`：会话记忆钩子
- `command-logger`：命令记录钩子
- `boot-md`：启动钩子

**自定义 Hooks：**
在工作区目录创建 `hooks/<hook-name>/handler.ts` 文件。

### 9. Node Host

Node Host 管理连接的节点（iOS/Android 设备）。

**主要功能：**
- 节点注册
- 节点发现
- 命令执行
- 事件订阅

### 10. Plugin SDK

Plugin SDK 允许开发者创建自定义插件。

**插件结构：**
```
my-plugin/
├── package.json
├── SKILL.md
└── src/
    └── index.ts
```

---

## 频道配置

### WhatsApp 配置

#### 1. 登录

```bash
moltbot channels login whatsapp
```

扫描显示的 QR 码以登录 WhatsApp。

#### 2. 配置选项

```json5
{
  channels: {
    whatsapp: {
      enabled: true,
      allowFrom: ["+1234567890"],  // 允许的电话号码列表
      dmPolicy: "pairing",          // DM 策略：pairing | open
      groups: {
        "*": {
          requireMention: true       // 群组中需要提及
        }
      },
      mediaMaxMb: 25               // 最大媒体文件大小（MB）
    }
  }
}
```

#### 3. DM 策略说明

- **pairing**：未知发送者会收到配对代码，需要批准
- **open**：所有消息都会被处理（需要设置 `allowFrom: ["*"]`）

#### 4. 批准配对

```bash
moltbot pairing approve whatsapp <code>
```

### Telegram 配置

#### 1. 创建 Bot

1. 在 Telegram 中与 [@BotFather](https://t.me/BotFather) 对话
2. 发送 `/newbot` 创建新机器人
3. 按照提示设置机器人名称和用户名
4. 复制获得的 Bot Token

#### 2. 配置选项

```json5
{
  channels: {
    telegram: {
      enabled: true,
      botToken: "123456:ABCDEF",
      allowFrom: [],                  // 允许的用户 ID 列表
      dmPolicy: "pairing",
      groups: {
        "*": {
          requireMention: true
        }
      },
      webhookUrl: "https://your-domain.com/webhook"
    }
  }
}
```

#### 3. 设置环境变量

```bash
export TELEGRAM_BOT_TOKEN="123456:ABCDEF"
```

### Slack 配置

#### 1. 创建 Slack App

1. 访问 [api.slack.com/apps](https://api.slack.com/apps)
2. 创建新应用
3. 配置 OAuth & Permissions
4. 安装到工作区
5. 复制 Bot Token 和 App Token

#### 2. 配置选项

```json5
{
  channels: {
    slack: {
      enabled: true,
      botToken: "xoxb-your-bot-token",
      appToken: "xapp-your-app-token",
      dmPolicy: "pairing",
      allowFrom: []
    }
  }
}
```

#### 3. 设置环境变量

```bash
export SLACK_BOT_TOKEN="xoxb-your-bot-token"
export SLACK_APP_TOKEN="xapp-your-app-token"
```

### Discord 配置

#### 1. 创建 Discord Bot

1. 访问 [discord.com/developers/applications](https://discord.com/developers/applications)
2. 创建新应用
3. 创建 Bot 用户
4. 复制 Bot Token
5. 配置 Gateway Intents

#### 2. 配置选项

```json5
{
  channels: {
    discord: {
      enabled: true,
      token: "your-discord-bot-token",
      dmPolicy: "pairing",
      allowFrom: [],
      guilds: {
        "guild-id": {
          requireMention: true
        }
      },
      mediaMaxMb: 25
    }
  }
}
```

#### 3. 设置环境变量

```bash
export DISCORD_BOT_TOKEN="your-discord-bot-token"
```

---

## 守护进程配置

### systemd（Linux）

#### 1. 服务文件位置

systemd 服务文件位于：
```
~/.config/systemd/user/moltbot.service
```

#### 2. 服务文件示例

```ini
[Unit]
Description=Moltbot Gateway
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/node /usr/local/lib/node_modules/moltbot/dist/gateway/server.js
Restart=always
RestartSec=10
Environment=CLAWDBOT_STATE_DIR=%h/.clawdbot
Environment=CLAWDBOT_WORKSPACE_DIR=%h/clawd

[Install]
WantedBy=default.target
```

#### 3. 管理命令

```bash
# 重新加载 systemd 配置
systemctl --user daemon-reload

# 启动服务
systemctl --user start moltbot

# 停止服务
systemctl --user stop moltbot

# 重启服务
systemctl --user restart moltbot

# 查看状态
systemctl --user status moltbot

# 启用开机自启
systemctl --user enable moltbot

# 禁用开机自启
systemctl --user disable moltbot

# 查看日志
journalctl --user -u moltbot -f

# 查看最近 100 行日志
journalctl --user -u moltbot -n 100
```

### launchd（macOS）

#### 1. plist 文件位置

launchd plist 文件位于：
```
~/Library/LaunchAgents/com.moltbot.daemon.plist
```

#### 2. plist 文件示例

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.moltbot.daemon</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/local/bin/node</string>
        <string>/usr/local/lib/node_modules/moltbot/dist/gateway/server.js</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>EnvironmentVariables</key>
    <dict>
        <key>CLAWDBOT_STATE_DIR</key>
        <string>/Users/yourname/.clawdbot</string>
        <key>CLAWDBOT_WORKSPACE_DIR</key>
        <string>/Users/yourname/clawd</string>
    </dict>
    <key>StandardOutPath</key>
    <string>/tmp/moltbot.out.log</string>
    <key>StandardErrorPath</key>
    <string>/tmp/moltbot.err.log</string>
</dict>
</plist>
```

#### 3. 管理命令

```bash
# 加载服务
launchctl load ~/Library/LaunchAgents/com.moltbot.daemon.plist

# 启动服务
launchctl start com.moltbot.daemon

# 停止服务
launchctl stop com.moltbot.daemon

# 卸载服务
launchctl unload ~/Library/LaunchAgents/com.moltbot.daemon.plist

# 查看服务状态
launchctl list | grep moltbot

# 查看日志
tail -f /tmp/moltbot.out.log
```

### Windows 任务计划程序

#### 1. 创建任务

```bash
# 使用 moltbot 命令安装
moltbot daemon install
```

#### 2. 手动配置

1. 打开"任务计划程序"
2. 创建基本任务
3. 设置触发器（启动时）
4. 设置操作（运行程序）
5. 程序路径：`node.exe`
6. 参数：`C:\Users\YourName\AppData\Roaming\npm\node_modules\moltbot\dist\gateway\server.js`
7. 设置条件（仅在计算机连接到网络时）
8. 设置设置（如果任务失败，每隔 1 分钟重新启动）

---

## 故障排除

### 常见问题

#### 1. Node.js 版本不兼容

**问题**：运行时提示 Node.js 版本过低

**解决方案**：
```bash
# 检查当前版本
node --version

# 如果版本 < 22.12.0，升级 Node.js
nvm install 22
nvm use 22
```

#### 2. 端口已被占用

**问题**：启动网关时提示端口 18789 已被占用

**解决方案**：
```bash
# 查找占用端口的进程
lsof -i :18789  # macOS/Linux
netstat -ano | findstr :18789  # Windows

# 终止进程
kill -9 <PID>  # macOS/Linux
taskkill /PID <PID> /F  # Windows

# 或使用其他端口
moltbot gateway --port 18889
```

#### 3. WhatsApp 登录失败

**问题**：无法登录 WhatsApp

**解决方案**：
```bash
# 删除现有凭据
rm -rf ~/.clawdbot/credentials/whatsapp.json

# 重新登录
moltbot channels login whatsapp
```

#### 4. 频道连接失败

**问题**：频道无法连接

**解决方案**：
```bash
# 检查网络连接
ping api.telegram.org
ping slack.com

# 检查配置
moltbot doctor

# 查看日志
journalctl --user -u moltbot -f
```

#### 5. 智能体无响应

**问题**：发送消息后智能体没有响应

**解决方案**：
```bash
# 检查 API 密钥配置
cat ~/.clawdbot/moltbot.json

# 检查模型配置
moltbot agent --status

# 查看详细日志
moltbot gateway --verbose
```

### 日志查看

#### systemd 日志

```bash
# 实时查看日志
journalctl --user -u moltbot -f

# 查看最近 100 行
journalctl --user -u moltbot -n 100

# 查看今天的日志
journalctl --user -u moltbot --since today

# 查看错误日志
journalctl --user -u moltbot -p err
```

#### launchd 日志

```bash
# 查看标准输出
tail -f /tmp/moltbot.out.log

# 查看错误输出
tail -f /tmp/moltbot.err.log
```

#### Docker 日志

```bash
# 查看容器日志
docker logs -f moltbot

# 查看最近 100 行
docker logs --tail 100 moltbot
```

### 诊断工具

#### 运行诊断

```bash
moltbot doctor
```

诊断工具会检查：
- Node.js 版本
- 配置文件
- 频道配置
- API 密钥
- 网络连接
- 权限设置

#### 检查配置

```bash
# 查看当前配置
moltbot config show

# 验证配置
moltbot config validate
```

#### 测试连接

```bash
# 测试网关连接
moltbot gateway --test

# 测试频道连接
moltbot channels test
```

---

## 安全建议

### 1. DM 访问控制

Moltbot 连接到真实的即时通讯界面，将入站 DM 视为**不受信任的输入**。

**推荐配置：**
```json5
{
  channels: {
    whatsapp: {
      dmPolicy: "pairing",  // 使用配对模式
      allowFrom: ["+1234567890"]  // 仅允许已知号码
    },
    telegram: {
      dmPolicy: "pairing",
      allowFrom: []
    },
    discord: {
      dmPolicy: "pairing",
      allowFrom: []
    }
  }
}
```

### 2. 群组安全

**群组配置建议：**
```json5
{
  channels: {
    whatsapp: {
      groups: {
        "*": {
          requireMention: true  // 群组中需要提及
        }
      }
    }
  }
}
```

### 3. 沙箱模式

对于群组/频道，建议使用沙箱模式：

```json5
{
  agent: {
    defaults: {
      sandbox: {
        mode: "non-main"  // 非主会话在 Docker 沙箱中运行
      }
    }
  }
}
```

**沙箱默认允许列表：**
- `bash`
- `process`
- `read`
- `write`
- `edit`
- `sessions_list`
- `sessions_history`
- `sessions_send`
- `sessions_spawn`

**沙箱默认拒绝列表：**
- `browser`
- `canvas`
- `nodes`
- `cron`
- `discord`
- `gateway`

### 4. API 密钥保护

**不要：**
- 将 API 密钥提交到版本控制
- 在公共聊天中分享密钥
- 使用弱密钥

**应该：**
- 使用环境变量存储密钥
- 定期轮换密钥
- 使用密钥管理服务

### 5. 网关认证

**配置网关认证：**
```json5
{
  gateway: {
    auth: {
      mode: "token",  // 或 "password"
      token: "your-strong-secret-token"
    }
  }
}
```

### 6. 网络暴露

**不要将网关暴露到公网，除非：**
- 使用强密码认证
- 使用 HTTPS/TLS
- 限制访问 IP

**推荐使用：**
- Tailscale Serve/Funnel
- SSH 隧道
- VPN

### 7. 定期更新

```bash
# 更新到最新版本
npm update -g moltbot@latest

# 或使用 pnpm
pnpm update -g moltbot@latest
```

### 8. 备份配置

```bash
# 备份配置文件
cp ~/.clawdbot/moltbot.json ~/.clawdbot/moltbot.json.backup

# 备份工作区
tar -czf clawd-backup-$(date +%Y%m%d).tar.gz ~/clawd
```

---

## 更新维护

### 更新到最新版本

#### 1. 检查当前版本

```bash
moltbot --version
```

#### 2. 更新全局安装

```bash
# 使用 npm
npm update -g moltbot@latest

# 使用 pnpm
pnpm update -g moltbot@latest
```

#### 3. 从源码更新

```bash
cd moltbot
git pull origin main
pnpm install
pnpm build
pnpm ui:build
```

### 开发频道切换

Moltbot 提供三个开发频道：

- **stable**：标记的发布版本（`vYYYY.M.D` 或 `vYYYY.M.D-<patch>`）
- **beta**：预发布标签（`vYYYY.M.D-beta.N`）
- **dev**：主分支的动态更新

**切换频道：**
```bash
# 切换到 stable 频道
moltbot update --channel stable

# 切换到 beta 频道
moltbot update --channel beta

# 切换到 dev 频道
moltbot update --channel dev
```

### 配置迁移

当更新到新版本时，可能需要迁移配置：

```bash
# 运行配置迁移
moltbot migrate

# 检查配置兼容性
moltbot doctor
```

### 数据备份

定期备份重要数据：

```bash
# 备份配置
cp -r ~/.clawdbot ~/.clawdbot.backup

# 备份工作区
cp -r ~/clawd ~/clawd.backup

# 备份凭据
cp -r ~/.clawdbot/credentials ~/.clawdbot/credentials.backup
```

### 数据恢复

从备份恢复数据：

```bash
# 停止服务
systemctl --user stop moltbot

# 恢复配置
cp -r ~/.clawdbot.backup/* ~/.clawdbot/

# 恢复工作区
cp -r ~/clawd.backup/* ~/clawd/

# 启动服务
systemctl --user start moltbot
```

---

## 附录

### A. 命令参考

#### moltbot 命令

```bash
# 网关命令
moltbot gateway [options]
moltbot gateway --port 18789 --verbose

# 智能体命令
moltbot agent [options]
moltbot agent --message "Hello" --thinking high

# 消息命令
moltbot message send --to +1234567890 --message "Hello"

# 频道命令
moltbot channels login <channel>
moltbot channels logout <channel>
moltbot channels status

# 配对命令
moltbot pairing approve <channel> <code>
moltbot pairing list

# 向导命令
moltbot onboard
moltbot onboard --install-daemon

# 诊断命令
moltbot doctor
moltbot status

# TUI 命令
moltbot tui
moltbot tui --dev

# 守护进程命令
moltbot daemon install
moltbot daemon uninstall
moltbot daemon start
moltbot daemon stop
```

### B. 环境变量完整列表

| 环境变量 | 说明 | 默认值 |
|---------|------|--------|
| `CLAWDBOT_STATE_DIR` | 状态目录 | `~/.clawdbot` |
| `CLAWDBOT_WORKSPACE_DIR` | 工作区目录 | `~/clawd` |
| `CLAWDBOT_GATEWAY_TOKEN` | 网关认证令牌 | - |
| `CLAWDBOT_SKIP_CHANNELS` | 跳过频道加载 | - |
| `CLAWDBOT_PROFILE` | CLI 配置文件 | - |
| `CLAWDBOT_NO_RESPAWN` | 禁用进程重生 | - |
| `CLAWDBOT_LIVE_TEST` | 启用实时测试 | - |
| `CLAWDBOT_NODE_OPTIONS_READY` | Node 选项就绪标记 | - |
| `TELEGRAM_BOT_TOKEN` | Telegram Bot Token | - |
| `SLACK_BOT_TOKEN` | Slack Bot Token | - |
| `SLACK_APP_TOKEN` | Slack App Token | - |
| `DISCORD_BOT_TOKEN` | Discord Bot Token | - |
| `ANTHROPIC_API_KEY` | Anthropic API Key | - |
| `OPENAI_API_KEY` | OpenAI API Key | - |
| `AWS_ACCESS_KEY_ID` | AWS Access Key ID | - |
| `AWS_SECRET_ACCESS_KEY` | AWS Secret Access Key | - |
| `AWS_REGION` | AWS Region | - |

### C. 目录结构

```
~/.clawdbot/                    # Moltbot 配置目录
├── moltbot.json                 # 主配置文件
├── credentials/                 # 凭据目录
│   ├── whatsapp.json           # WhatsApp 凭据
│   ├── telegram.json           # Telegram 凭据
│   ├── slack.json              # Slack 凭据
│   └── discord.json           # Discord 凭据
├── state/                      # 状态目录
│   ├── sessions/               # 会话状态
│   ├── channels/               # 频道状态
│   └── gateway/               # 网关状态
└── logs/                       # 日志目录

~/clawd/                        # 工作区目录
├── AGENTS.md                   # 智能体配置
├── SOUL.md                     # 智能体灵魂
├── TOOLS.md                    # 工具配置
├── skills/                     # 技能目录
│   └── <skill>/
│       └── SKILL.md
└── sessions/                   # 会话数据
```

### D. 端口使用

| 端口 | 用途 | 说明 |
|------|------|------|
| 18789 | Gateway WebSocket | 网关 WebSocket 端口 |
| 8080 | HTTP Server | HTTP 服务器端口（可选） |
| 3000 | Control UI | 控制界面端口（可选） |

### E. 支持的模型

#### Anthropic Claude

- `anthropic/claude-opus-4-5`
- `anthropic/claude-sonnet-4-5`
- `anthropic/claude-haiku-4-5`

#### OpenAI

- `openai/gpt-4`
- `openai/gpt-4-turbo`
- `openai/gpt-3.5-turbo`

#### AWS Bedrock

- `bedrock/anthropic.claude-3-opus-20240229-v1:0`
- `bedrock/anthropic.claude-3-sonnet-20240229-v1:0`
- `bedrock/anthropic.claude-3-haiku-20240307-v1:0`

### F. 相关链接

- **官网**：https://molt.bot
- **文档**：https://docs.molt.bot
- **GitHub**：https://github.com/moltbot/moltbot
- **Discord**：https://discord.gg/clawd
- **DeepWiki**：https://deepwiki.com/moltbot/moltbot

### G. 联系方式

- **邮箱**：purpose168@outlook.com
- **Discord**：https://discord.gg/clawd
- **GitHub Issues**：https://github.com/moltbot/moltbot/issues

---

## 许可证

MIT License

---

**文档版本**：1.0.0
**最后更新**：2026-01-31
**维护者**：Moltbot Team
